# Suggestions

- Consider sorting upgrade rows within each section by progression priority instead of static file order.
- Recommended sort order: `Ready -> Need cash -> Locked -> Purchased`.
- Alternative sort orders to revisit later:
  - `Locked -> Ready -> Need cash -> Purchased`
  - keep static file order
